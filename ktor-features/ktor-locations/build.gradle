description = ''
dependencies {
    compile project(':ktor-features:ktor-auth')
}

dokkaJavadoc.samples = [
        'test/io/ktor/tests/locations/LocationSamples.kt',
]

dokka {
    samples = [
            'test/io/ktor/tests/locations/LocationSamples.kt',
    ]
}
